<UserControl x:Class="lab05.Views.CategoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="ID:" />
            <TextBox Text="{Binding CategoriaID}" Width="50" IsEnabled="False" Margin="5"/>
            <Label Content="Nombre:" />
            <TextBox Text="{Binding NombreCategoria}" Width="150" Margin="5"/>
            <Label Content="Descripción:" />
            <TextBox Text="{Binding Descripcion}" Width="250" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Insertar" Command="{Binding InsertarCommand}" Width="80" Margin="5"/>
            <Button Content="Actualizar" Command="{Binding ActualizarCommand}" Width="80" Margin="5"/>
            <Button Content="Eliminar (Lógico)" Command="{Binding EliminarCommand}" Width="110" Margin="5" Background="#FFF44336" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Categorias}" AutoGenerateColumns="True" IsReadOnly="True" />
    </Grid>
</UserControl>